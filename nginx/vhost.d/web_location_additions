# This is symlinked in docker-compose.* to the file corresponding to each vhost ;
# as described here https://github.com/nginx-proxy/nginx-proxy/issues/1398#issuecomment-587717134
# it's then merged into the location block generated by nginx-proxy.

limit_req zone=api_rate_limit burst=20 nodelay;
add_header RateLimit-Limit 20;
add_header RateLimit-Policy "q=20;w=1";
limit_req_status 429;